# Server-only. Never prefix with NEXT_PUBLIC_.
NEWTON_API_KEY=
PRIVATE_KEY=

# Optional public RPC for wagmi / the curator Route Handler.
# Base Sepolia default if unset: https://base-sepolia-rpc.publicnode.com
NEXT_PUBLIC_RPC_URL=
RPC_URL=
